enum NoteColor { blue, yellow, grey, orange, green, red }

enum NoteTag { none, home, work, school }

enum NoteReminder { none, some }

enum NoteReminderChannel { reminderChannelKey }

enum Filter { none, alphabet, date }
